describe('Position', () => {
    describe('generateRandomPosition', () => {
        it('should return a valid position', () => {
            // Arrange
            

            // Act

            // Assert
        })
    })
})